<h1>販売履歴一覧</h1>

<hr>

<% @order.each do |order| %>
  <p>注文ID：<%= order.id %></p>
  <p>ユーザーID：<%= order.user_id %></p>
  <p>ユーザーニックネーム：<%= order.user.nickname %></p>
  <p>合計金額：<%= order.total_price %></p>
  <p>支払方法：<%= order.payment_method_i18n %></p>
  <hr>
<% end %>
