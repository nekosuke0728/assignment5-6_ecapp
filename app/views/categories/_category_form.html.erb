<%= form_for(category) do |f| %>

  <% @category.errors.full_messages.each do |message| %>
    <div class="alert alert-danger" role="alert">
      <li><%= message %></li>
    </div>
  <% end %>

  <p>カテゴリ名：<%= f.text_field :name %></p>
  <%= f.submit %>
<% end %>

<%= link_to '戻る', :back %> 